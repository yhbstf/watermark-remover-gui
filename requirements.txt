opencv-python>=4.5
numpy>=1.21
pillow>=9.0
python-docx>=0.8.11
iopaint>=1.3.0

# Optional (enable extra features):
# pip install pymupdf           # PDF support
# pip install pytesseract       # OCR text watermark detection (requires Tesseract binary)
# pip install tkinterdnd2       # Drag-and-drop files into the window
